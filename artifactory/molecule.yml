---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: centos7
    image: quay.io/centos/centos:7
    privileged: true
    pre_build_image: true
    volumes:
     - /sys/fs/cgroup:/sys/fs/cgroup:ro
provisioner:
  name: ansible
verifier:
  name: ansible

